try {
    var parsedData = JSON.parse(data);
    log = "";
    var fullDetails = SceneLoader.loggingLevel === SceneLoader.DETAILED_LOGGING;

    var index: number;
    var cache: number;
    // Lights
} catch (err) {
    let msg =  log;
        throw err;
} finally {
}