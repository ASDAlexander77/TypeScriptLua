import './JS';

function ffff(f1:? number) {
    if (f1 != undefined) {
        console.log("works");
    } else {
        console.log("<error>");
    }

    console.log(f1);
}

ffff(0);

